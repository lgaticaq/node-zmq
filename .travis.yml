sudo: required

services:
  - docker

env:
  matrix :
    - env:
      VERSION: '8.8.1'
    - env:
      VERSION: '6.11.5'
    - env:
      VERSION: '4.8.5'

install:
  - docker build -t "lgatica/node-zmq:$VERSION" "$VERSION"

script:
  - docker run --rm "lgatica/node-zmq:$VERSION" node --version
